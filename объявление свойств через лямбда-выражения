возможность объявлять свойства через лямбда-выражения появилась в  C# 6.0.
слева от => записывается имя свойства, а справа - его геттер
например
public bool isGood => 2 > 3;
эквивалентно
public bool isGood {
  {
    return 2 > 3;
  }
}
при получении свойства будет осуществлена проверка 2 > 3 и вернётся соответствующее логическое значение (в данном случае всегда false)

также в версии C# 6.0 стало возможным инициализировать свойства значением при объявлении
